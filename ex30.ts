let name1:string[]=["chali","sweeto","zarra","admin"]
for(let names of name1){
    if(names==="admin"){console.log(`Hallow ${names},would you lik to see the reports`);
    }else{console.log(`Hallow ${names},you for logging again`);
    }
}